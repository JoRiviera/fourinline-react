import React from 'react';
import ReactDOM from 'react-dom';
import { App } from './App';

import './App.css';

ReactDOM.render(
  <React.StrictMode>
    <h1 className="mainTitle">Four in one line</h1>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
